# Anthropic Provider

<instructions>
Provider for Anthropic Claude usage.
</instructions>

## Patterns
- OAuth auth flow via `auth.ts`.
- Parallel fetching of profile and usage data in `fetch.ts`.
- Plan inference logic in `parse.ts`.
